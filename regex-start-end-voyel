SELECT DISTINCT CITY 
FROM STATION
WHERE 
    LOWER(SUBSTR(CITY, 1, 1)) IN ('a', 'e', 'i', 'o', 'u') AND #GRABBING THE FIRST CHARACTER OF THE WORD
    LOWER(SUBSTR(CITY, LENGTH(CITY), 1)) IN ('a', 'e', 'i', 'o', 'u'); # GRABBING THE LAST CHARACTER OF THE WORD.

SUBSTR(DATA,LENGTH,POSITION) --> BASICALLY I WANT YOU TO EXTRACT THE FIRST CHARACTER POSITION FROM THIS WORD OF LENGHT X.
